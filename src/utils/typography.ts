import { scaleFont } from './mixins'

// FONT SIZE
export const SIZE = {
    normal: scaleFont(14),
    smaller: scaleFont(12),
    bigger: scaleFont(16),
    
    faNormal: scaleFont(16),
    faSmaller: scaleFont(14),

    navbarTitle: scaleFont(18),
}
